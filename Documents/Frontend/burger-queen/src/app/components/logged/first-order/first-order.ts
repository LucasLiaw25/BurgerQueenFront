import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-first-order',
  imports: [RouterLink],
  templateUrl: './first-order.html',
  styleUrl: './first-order.css',
})
export class FirstOrder {

}
